<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.knowly</groupId>
        <artifactId>knowly-server</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>knowly-ingest</artifactId>
    <name>knowly-ingest</name>
    <description>摄取层实现：TikaDocumentParser / PdfDocumentParser / RuleBasedLayoutAnalyzer / TesseractOcrEngine</description>

    <dependencies>
        <!-- 内核依赖 -->
        <dependency>
            <groupId>com.knowly</groupId>
            <artifactId>knowly-core</artifactId>
        </dependency>

        <!-- Apache Tika（文档解析） -->
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-parsers-standard-package</artifactId>
        </dependency>

        <!-- PDFBox（PDF 文本提取 + 坐标，版面分析用） -->
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
        </dependency>

        <!-- Tesseract OCR -->
        <dependency>
            <groupId>net.sourceforge.tess4j</groupId>
            <artifactId>tess4j</artifactId>
        </dependency>
    </dependencies>
</project>
