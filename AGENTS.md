# 知了 knowly · 项目工作准则

> 本文件是知了项目的**硬约束和检查清单**，配合全局 `~/.zcode/AGENTS.md`（架构师心法）使用。
> 全局准则讲"怎么思考"，本文件讲"知了项目具体怎么做"。
> 两者叠加生效。每次做设计/写代码前，必须对照本清单自检。

---

## 一、项目定位（不可妥协的边界）

知了是**开源的数据 AI 化清洗工具**。以下边界是产品根基，任何功能设计不得违反：

- **一次性清洗工具，不是 RAG 平台**：用户心智是"选文件→清洗→拿产出→完事"。**不要"知识库"概念**，不做文档管理、不做知识库 CRUD。借鉴竞品时必须基于知了自身定位，不是竞品有什么就搬什么。
- **双入口：CLI 为主 + Web 为调优工作台**：
  - **CLI（knowly-cli）**：主入口，负责批处理、自动化、脚本编排、服务器环境。配置驱动（YAML），可被别的系统调用。
  - **Web（knowly-web）**：**定位是"清洗调优工作台"，不是管理平台**。存在的价值是可视化交互——让"看进度、预览产出、调分段效果"这几件 CLI 做不好的事体验更好。Web **不做**知识库管理、文档存储、完整历史归档、多用户协作等平台功能。
  - **共享内核**：CLI 和 Web 都调 knowly-core，配置 YAML 是两者的桥梁（Web 调好的配置可导出给 CLI 用）。
  - 设计任何 Web 功能前，先问："这个功能 CLI 做不了或做不好吗？"如果 CLI 能做，就不要放进 Web 增加复杂度。
- **纯造库，不做查询**：只负责把文档清洗成知识产物（Markdown/JSONL/向量），不负责 RAG 查询。查询是消费方的事。
- **通用，不绑领域**：中医/命理只是首个验证场景，不是假设。任何设计不得硬编码领域逻辑。
- **产出不绑存储**：清洗结果可落向量库（Qdrant/PgVector 可选），也可只产 Markdown/JSONL。向量库是可选 Sink，不配不装。
- **仅本机使用**：v0.1 不支持远程访问。文件直接选本机路径，不上传。前端 API 写死 localhost。
- **可插拔**：LLM/embedding/向量库/图库/产出格式都是 SPI 接口，默认实现可覆盖。
- **开源友好**：Apache 2.0；核心轻量，重依赖（OCR/ASR）可选；测试样例零版权（自造，脚本生成，不入库）。

> 当一个需求看似合理但违反上述边界时，**优先守边界**。在文档里说明"为什么不做"。

---

## 二、技术栈约束

- **核心语言**：Java 17+ / Spring Boot 3.x
- **内核纯净**：`knowly-core` 不依赖任何 Web 框架（无 Spring MVC / HTTP / 连接池）。CLI、Web、第三方嵌入都能复用。
- **Python 后路**：v0.1 纯 Java。未来 LLM 密集功能（图谱/semantic chunking）若 Python 生态优势明显，可子进程/gRPC 调 Python 模块。相关 SPI 接口预留。
- **前端**：React + Vite（独立工程 `knowly-web`），API 地址写死 `localhost:8080`，不支持远程。
- **核心存储**：SQLite（必装，文件型零运维）——存 admin 账号/当前任务状态/断点续跑状态/设置。不存清洗历史（工具不做历史归档）；配置模板走本地 YAML 文件。
- **向量库（可选）**：Qdrant（默认推荐）或 PgVector 二选一。运行时按需选，开发环境都装。不配向量库只产 Markdown/JSONL。
- **默认 embedding**：DashScope `text-embedding-v3` / 1024 维（固化，影响向量库维度）。换模型必须同步改维度并重建。
- **用户体系**：v0.1 单 admin 账号（JWT 登录保护 Web UI）；多用户放 v0.2（表结构已预留）。

---

## 三、五层设计检查清单（知了专用）

> 每个设计决策、每个新功能，必须过这张表。不过表的设计，打回重做。

### 第一层 功能正确性
- [ ] 数据流是否清晰：原始文件 → RawDocument → TextChunk → [EmbeddedChunk] → Sink(s)？
- [ ] 这个功能在不在边界内（见第一节）？不在边界内的，写明"为什么不做"。
- [ ] 异常路径：单文件失败是否隔离？空输入/超大输入/损坏文件怎么处理？

### 第二层 非功能质量
- [ ] **性能**：并发模型？背压（有界队列）？大文件流式读取？限流器（令牌桶）？批量写入？
- [ ] **可靠**：重试（`RetryTemplate`，指数退避+抖动）？超时分层？断点续跑（文件×阶段粒度）？Sink 幂等（按 chunk id）？
- [ ] **安全**：文件上传白名单（扩展名+magic number）+ 大小上限 + 路径校验（`PathSanitizer`）？密钥脱敏（`SecretMasker`，API key 不进日志/报告）？SQL 参数化（PreparedStatement）？YAML SafeConstructor？
- [ ] **健壮**：优雅停机（shutdown hook + flush sink + 标记状态）？跨 sink 一致性（补偿事务）？资源生命周期（AutoCloseable）？

### 第三层 可演化性
- [ ] 新格式/新向量库/新分段策略，是否只需加 SPI 实现、不改核心？
- [ ] TextChunk/JSONL/配置 schema 是否前向兼容（加字段不破坏老消费方）？
- [ ] 有测试策略？样例覆盖（用 `knowly-dist/examples/samples/generate_samples.py`）？

### 第四层 可运营性
- [ ] **可观测**：结构化日志（JSON）？指标埋点（处理速率/失败率/队列深度/各阶段耗时）？进度事件（`PipelineEvent` 观察者）？
- [ ] **可部署**：可执行 jar？Docker？依赖引导（Tesseract/PostgreSQL/字体）？健康检查端点？配置外部化（环境变量）？
- [ ] **故障诊断**：单文件单阶段能否重跑调试？ErrorCollector 失败明细够不够定位？

### 第五层 产品与体验
- [ ] 不配任何数据库时，能否默认产 Markdown（零运维 aha moment）？
- [ ] 错误信息是否指出"哪个文件/哪个阶段/什么错/怎么处理"？
- [ ] **Web 功能闸**：这个 Web 功能 CLI 做不了或做不好吗？能 CLI 就不进 Web。Web 是清洗调优工作台，不是管理平台。
- [ ] 借鉴竞品时，是否基于知了自身定位过滤（不是竞品有什么就搬什么）？

### 横切
- [ ] 公共方法是否抽到 `knowly-common`（HashUtils/RetryTemplate/RateLimiter/TextNormalizer/PathSanitizer/SecretMasker）？
- [ ] 设计模式是否恰当应用（工厂+注册表 / 观察者 / 责任链 / 适配器 / 模板方法 / 策略 / 建造者）？
- [ ] 命名/异常/日志/配置风格是否统一？

---

## 四、架构硬约束（不可违反）

1. **模块依赖方向**：所有实现层依赖 `knowly-core` 的接口和数据模型，**模块之间不直接依赖**（通过 core 接口交互）。
2. **组件实例化走工厂+注册表**：YAML 里的 `type: pgvector` 通过 `SinkRegistry.create(type, config)` 实例化，不硬编码 switch-case。
3. **进度通知走观察者**：`PipelineEngine` 发布 `PipelineEvent`，CLI/Web 各注册 listener。内核不知道有 CLI 还是 Web。
4. **清洗步骤走责任链**：cleaner 串联执行，可配置顺序。
5. **向量化与落库解耦**：embed 阶段只产 `EmbeddedChunk`，落库交给 `ChunkSink`。是否启用 embed 取决于有无 `requiresEmbedding()` 的 sink。
6. **公共逻辑必须先抽再写业务**：发现重复，先抽到 `knowly-common`，不要复制粘贴。
7. **设计模式不过度**：能用简单方案解决的，不引入模式。引入模式必须在 ADR 说明理由。

---

## 五、文档体系（8 份，定位分明）

> 排序依据：读者从头到尾的自然认知递进。决策依据文档排在引用它的文档之前。
> **文档归属原则**：规范跟着它服务的工程走（后端规范在 knowly-server 下，前端规范在 knowly-web 下），不放根 docs——这样独立发版不丢失、clone 即可见。

```
docs/
├── 01-立项背景.md           # 为什么做、定位、价值（所有人）
├── 02-竞品调研报告.md        # 决策依据：市场缺口 + 技术借鉴（支撑立项和技术选型）
├── 03-需求分析.md      # 做什么（功能 + 非功能可验收指标）
├── 04-系统架构.md           # 大方向、技术选型、模块划分、约束、ADR（讲 What & Why）
├── 05-开发规范.md           # 先立规矩（前后端共享）：命名/注释/Git/CR/测试原则/跨端安全铁律
├── 06-后端开发文档.md        # 后端开发设计：类/接口/DDL/算法实现/配置schema/工程目录（讲 How）
├── 07-部署文档.md           # 把它跑起来：环境搭建/依赖/打包/启动/Docker
└── 08-测试文档.md           # 验证：测试策略/样例说明/覆盖率/用例规范

# 开发流程：先后端开发（06）→ 后端完成 → 整理接口对接文档给前端 → 前端照着做
# 不单独写前端开发文档；接口对接文档等后端完成后单独整理（编号届时定）

knowly-server/docs/
└── 开发规范.md               # 后端专属（Java）：风格/异常错误码/日志/编码实践/依赖/配置/安全/并发/API设计/性能/测试

knowly-web/docs/
└── 开发规范.md               # 前端专属（React+TS）：风格/错误处理/日志/组件设计/依赖/配置/安全/性能/测试
```

**命名规范**：两位数字序号 + 中划线 + 中文文档名。两位数字预留扩展空间。
**后续按需**：接口对接文档（后端完成后）、使用手册（Web UI 完成后）、运维手册（生产部署时）。

**铁律**：
- 系统架构（04）**不放代码骨架**。类名/方法签名/DDL/工程目录树全部放后端开发文档（06）。
- 架构讲"为什么这样设计、有什么约束、权衡了什么"；后端开发文档讲"具体怎么写"。
- 开发规范（05）在后端开发文档（06）之前——先立规矩，再教怎么写。
- 后端开发文档只负责后端开发设计，**不含接口契约**。接口对接文档单独整理。
- 前端不单独写开发文档，照接口对接文档做页面。
- 开发规范按工程归属：共享部分在 docs/05-开发规范.md，前后端专属部分在各自工程的 docs/ 下。
- 每个关键决策必须有 ADR（架构附录），写清：背景 / 选项 / 选择 / 理由 / 代价 / 失效场景。

---

## 六、公共工具类清单（knowly-common 必须提供）

> 凡是以下能力，必须先实现公共类，业务代码复用，禁止重复实现。

| 工具类 | 职责 | 设计要点 |
|---|---|---|
| `HashUtils` | 文件内容哈希、chunk id 生成（`documentId#ordinal`） | SHA-256；documentId 由 contentHash 派生（路径变化也稳定） |
| `RetryTemplate` | 统一重试（max/backoff/jitter/超时/可重试异常） | 指数退避+抖动防惊群；所有可重试操作复用 |
| `RateLimiter` | 统一限流（令牌桶） | embedding API 等 QPS 受限场景 |
| `TextNormalizer` | 文本规范化（全角转半角/繁简统一/去空白） | 中文场景默认开繁简统一 |
| `EncodingDetector` | 编码检测与转换（GBK→UTF-8） | |
| `PathSanitizer` | 路径安全校验（防穿越） | `Path.normalize()` 后校验在白名单目录内 |
| `SecretMasker` | 敏感信息脱敏 | API key 永不进日志/报告/异常 |

---

## 七、自检（每次交付前必做）

- [ ] 我是在应付表象，还是解决了本质问题？
- [ ] 有没有过度设计？模块/功能/配置能不能删？
- [ ] 五层检查表（第三节）是否逐项过了？
- [ ] 公共方法抽了吗？设计模式用对了吗（没过度）？
- [ ] 契约（TextChunk/JSONL/配置）的前向兼容考虑了吗？
- [ ] 错误信息对人友好吗？
- [ ] 文档放对层级了吗（设计 vs 开发）？
- [ ] 有没有用模糊话术掩盖没想清楚的地方？标注 `[待验证]` `[假设]` `[风险]` 了吗？
